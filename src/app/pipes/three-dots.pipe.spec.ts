import { ThreeDotsPipe } from './three-dots.pipe';

describe('ThreeDotsPipe', () => {
  it('create an instance', () => {
    const pipe = new ThreeDotsPipe();
    expect(pipe).toBeTruthy();
  });
});
